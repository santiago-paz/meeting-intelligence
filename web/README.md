# web

The Next.js app for Meeting Intelligence: the meetings, ask and traces pages.
Setup, architecture and the reasoning live in the [root README](../README.md).

```bash
npm install && npm run dev
```

```bash
npm test
```

The app expects the API at `http://localhost:8000`; set `API_URL` to point it
elsewhere.
